
创建项目库
CREATE DATABASE `project` CHARACTER SET utf8 COLLATE utf8_general_ci;


创建用户表：
create table users(
    `user_id` int not null,
    `user_name` varchar(64) ,
    `user_password` varchar(64),
    `user_mail` varchar(64),
    `user_account_name` varchar(64),
    `create_time` timestamp DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',
    `update_time` timestamp DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
     PRIMARY KEY (user_id)
);

不可重复字段设置
ALTER TABLE 数据库.表 ADD UNIQUE (设置不能重复的字段);

`id` int primary key not null auto_increment,

创建主页的数据表：
reptile/fuun.go
db.CreateTable(&User{})


创建帖子表
create table post(
    `post_id` int  not null ,
    `theme` varchar(128),
    `introduce` varchar(1024),
    `head_portrait` longblob,
    `release_time` timestamp DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',   
    `update_time` timestamp DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
     PRIMARY KEY(post_id)
);



创建帖子内的内容表
create table post_content(
    `user_id` int not null,
    `post_id` int not null,
    `content` varchar(1024),
    `release_time` timestamp DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',   
    `update_time` timestamp DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
     PRIMARY KEY(user_id,post_id)
);


创建帖子

create table stick_data(
    `id` int primary key not null auto_increment,
    `title` varchar(128),
    `data` text,
    `create_time` timestamp DEFAULT CURRENT_TIMESTAMP COMMENT '发布时间',   
    `update_time` timestamp DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
)


#删除id后 重新初始化id
alter table user3 drop id;
alter table user3 add id int not null primary key auto_increment first;